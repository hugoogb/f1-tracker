# Database (local Docker PostgreSQL)
DATABASE_URL=postgresql://f1tracker:f1tracker_dev@localhost:5432/f1tracker

# Neon (production) — used by scripts/update-neon.sh and the scheduled ingest workflow.
# Full form includes the SSL param: postgresql://user:pass@ep-xyz.region.aws.neon.tech/f1tracker?sslmode=require
NEON_DATABASE_URL=

# FastAPI
FASTAPI_HOST=0.0.0.0
FASTAPI_PORT=8000
FASTAPI_DEBUG=true

# CORS — comma-separated list of allowed frontend origins
CORS_ORIGINS=http://localhost:3000

# f1db — the dataset release to ingest. 'latest' tracks the newest release;
# pin a tag (e.g. v2026.5.0) for reproducible seeds.
F1DB_VERSION=latest
F1DB_CACHE_DIR=.f1db_cache

# Fast-F1 (session timing, 2018+)
FASTF1_CACHE_DIR=.fastf1_cache

# Next.js (baked into the bundle at build time — set before `pnpm build`)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Shared secret for POST /api/revalidate (cache purge). Must match the Vercel env
# var and the REVALIDATE_SECRET GitHub Actions secret used by the ingest workflow.
REVALIDATE_SECRET=
REVALIDATE_URL=https://f1-tracker-web.vercel.app/api/revalidate
